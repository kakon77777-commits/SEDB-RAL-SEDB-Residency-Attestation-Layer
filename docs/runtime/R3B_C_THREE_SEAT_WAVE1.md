# R3B-C Three-Seat Wave 1 Candidate

SEDB-RAL `0.5.0c1` contains the synthetic code candidate for a public-only,
equal-standing three-seat registration wave. The candidate proves deterministic
ordering, independent application approvals, per-slot execution authority,
append-only policy state, strict H0-to-H3 sequencing, durable recovery states,
public RAL readback, and fail-closed effect boundaries.

It does not register a real applicant. Current applicant claims remain
candidate-only until an exact completed assistant output item is host-visible,
digest-bound to its task and turn, separately approved by the principal, and
accepted by a later operational plan. Admission order carries no rank,
historical superiority, continuity, or authority.

## Current boundary

```text
production_wave_run = NOT_RUN
live_limen_b6a = NOT_RUN
production_root_status = NOT_READ
pinned_p3_4_receipt = VERIFIED
r3b_b_regression = PASS
```

The production-root statement means the Wave candidate did not open the live
registry. The P3-4 and R3B-B statements refer to pinned public evidence and the
separate synthetic regression, not to a fresh production observation.

The private B6B gate remains disabled. The candidate neither reads nor writes a private
Residence, memory body, provider state, network transport, Fabric, MCP, cloud,
or external CLI. A public RAL readback bundle is not a LIMEN resolution and is
not identity, continuity, private access, or execution authority.

## Acceptance

The deterministic gate covers W1-001 through W1-053. Forty-seven RAL-owned
controls execute in disposable storage. The six LIMEN-owned currentness and
cross-task controls remain explicitly `NOT_RUN_OWNER_PLAN_REQUIRED`; they may
only run under a separately reviewed B6A plan.

The positive path records exact runtime references for nine fixture reads,
nineteen staging writes, twelve ledger events, and four synthetic receipts.
All production, private, network, provider, Fabric, MCP, and external-CLI
dimensions remain zero. Independent injected controls prove each forbidden
dimension would turn the gate red.

## Next gates

1. Write and review a LIMEN B6A plan for fresh post-append observations.
2. Write and review a real operational Wave plan with exact applicant evidence,
   application approvals, policy activation, per-slot JIT authority, and manual
   stop points.
3. Only then collect fresh claims and consider canonical admission.

No part of this runbook grants those later authorities.
